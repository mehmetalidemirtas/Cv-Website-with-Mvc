@using MvcCvProject.Models.Entity
@model List<Table_SocialMedia> 
<div class="social-icons">
    @foreach (var x in Model)
    {
    <a class="social-icon" href="@x.Link"><i class="@x.Icon"></i></a>
    }
</div>
